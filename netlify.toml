# Netlify deploy config for Mini E-Commerce (Nuxt 4 + Nitro + NuxtHub + Turso)
[build]
  command = "node scripts/ensure-libsql-linux.mjs && pnpm build:netlify && node scripts/fix-symlinks.mjs"
  publish = "dist"

[build.environment]
  NODE_VERSION = "24"

[functions]
  node_bundler = "esbuild"
